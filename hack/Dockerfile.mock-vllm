FROM python:3.12-slim

WORKDIR /app

# Install Flask (no requirements.txt needed — only one dep).
RUN pip install --no-cache-dir flask==3.0.3

COPY hack/mock-vllm.py /app/mock-vllm.py

EXPOSE 8000

ENV PYTHONUNBUFFERED=1

CMD ["python", "mock-vllm.py"]
